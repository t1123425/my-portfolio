// eslint-disable-next-line prettier/prettier
import React, { memo } from 'react'

const Work: React.FC = () => {
  return (
    <section>
      <h1>Work</h1>
    </section>
  )
}

export default memo(Work);
